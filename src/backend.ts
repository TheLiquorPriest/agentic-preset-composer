// Current staging provides reusable generic primitives—top-level metadata passthrough,
// native preset-editor helpers, assembly, quiet generation, longer interceptor budgets,
// richer pre-assembly context, and post-assembly mutation—but not canonical APC.
// Keep this entrypoint inert: register no APC APIs until all six complementary gates merge
// and matching types publish. Local bundle preparation grants no APC runtime authority.
// Portable data will use metadata.agentic_preset_composer; see DESIGN.md and AUTHOR_BRIEF.md.
export {}
